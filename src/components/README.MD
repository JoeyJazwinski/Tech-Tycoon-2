Our React Components will live here

-   Run 'yarn', this will install babel-cli@6 babel-preset-react-app@3
-   Next run 
    npx babel --watch src/components --out-dir src/compiled --presets react-app/prod
    This will compile JSX to normal JS